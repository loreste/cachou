# Sandbox files root

This directory is the default root for the demo Files API (`CACHOU_FILES_ROOT`).

It intentionally avoids exposing the full repository. Place demo files you want to browse here.
