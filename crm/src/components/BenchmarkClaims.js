// Generated by CachouJS Compiler (JS) - DO NOT EDIT
// Source: BenchmarkClaims.cachou
import * as Cachou from "cachoujs";
if (typeof document !== "undefined") import("./BenchmarkClaims.css");

const {
  signal,
  effect,
  createRoot,
  memo,
  store,
  batch,
  onCleanup,
  onMount,
  untrack,
  getOwner,
  runWithOwner,
  html,
  mapArray,
  createResource,
  htmlStatic,
  Router,
  Route,
  Layout,
  Outlet,
  Link,
  navigate,
  getPath,
  getQueryParams,
  getRouteData,
  useRouteData,
  useParams,
  useSearchParams,
  useHead,
  Show,
  Switch,
  Match,
  For,
  Index,
  splitProps,
  mergeProps,
  Dynamic,
  createAction,
  redirect,
  notFound,
  createMutation,
  persist,
  Dialog
} = Cachou;

export default function BenchmarkClaims(props = {}) {
  // --- Component Setup ---


  // --- Render ---
  return html`
<section class="claims" data-c-benchmarkclaims>
  <header data-c-benchmarkclaims>
    <span data-c-benchmarkclaims>Competitive proof</span>
    <h2 data-c-benchmarkclaims>Where Cachou is targeting number one</h2>
  </header>
  <div class="claim-grid" data-c-benchmarkclaims>
    ${props.claims.map(claim => html`<article data-c-benchmarkclaims>
      <span data-c-benchmarkclaims>${claim.category}</span>
      <strong data-c-benchmarkclaims>${claim.rank}</strong>
      <h3 data-c-benchmarkclaims>${claim.title}</h3>
      <p data-c-benchmarkclaims>${claim.note}</p>
    </article>`)}
  </div>
  <div class="history" data-c-benchmarkclaims>
    <span data-c-benchmarkclaims>History</span>
    <strong data-c-benchmarkclaims>${props.historySummary}</strong>
    <p data-c-benchmarkclaims>${props.historyNote}</p>
  </div>
  ${props.trends && props.trends.length ? html`<div class="trend-grid" data-c-benchmarkclaims>
    ${props.trends.map(trend => html`<article class="trend" data-c-benchmarkclaims>
      <span data-c-benchmarkclaims>${trend.scenario}</span>
      <strong data-c-benchmarkclaims>${trend.rank}</strong>
      <h3 data-c-benchmarkclaims>${trend.movement}</h3>
      <p data-c-benchmarkclaims>${trend.p95} - ${trend.p95Delta}</p>
    </article>`)}
  </div>` : null}
</section>
`;
}
//# sourceMappingURL=BenchmarkClaims.js.map
